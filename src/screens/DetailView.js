import * as React from 'react';
import { Text, View } from 'react-native';

export default Detailview = (item) => {
    return (
        <View>
            <Text>
                Hello from the details screen
            </Text>
        </View>
    );
};